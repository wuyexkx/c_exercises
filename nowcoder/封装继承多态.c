#if 0
filename="$0"
name="${filename%.*}"
CC="gcc"
$CC -o name filename 
./name &@
rm name
exit
#endif


// 有纯虚函数的类叫做抽象类(abstract class)，它不能生成对象。

// 用基类指针访问虚函数:
//  这时虽然大家都是Animal*，但是调用的Speak函数却是各自的类实现的Speak。
//  用哪个子类初始化基类指针就可以调用对应子类实现的虚函数。



#include <stdio.h>

int main()
{
    char 
    sscanf("A,0.444,333333,3.1415926",);
    printf("=========\n");
    return 0;
}